@model Ensco.Irma.Models.PobHomeModel

@Html.DevExpress().Splitter(settings =>
{
    settings.Name = "pobHomeSplitter";
    settings.AllowResize = false;
    settings.Orientation = System.Web.UI.WebControls.Orientation.Vertical;
    settings.SeparatorVisible = false;
    settings.Styles.Pane.Border.BorderWidth = System.Web.UI.WebControls.Unit.Pixel(0);
    settings.Styles.Pane.Paddings.Padding = System.Web.UI.WebControls.Unit.Pixel(0);
    settings.Styles.Pane.Border.BorderStyle = System.Web.UI.WebControls.BorderStyle.None;

    settings.Panes.Add(pane =>
    {
        pane.Name = "panelPobOveview";
        pane.AutoHeight = true;
        pane.PaneStyle.BorderBottom.BorderWidth = System.Web.UI.WebControls.Unit.Pixel(1);
        pane.AutoWidth = false;
        pane.Size = Unit.Percentage(100);

        pane.SetContent(() =>
        {
            Html.RenderAction("PobOverviewPartial", "Personnel", Model);
        });
    });
    settings.Panes.Add(pane =>
    {
        pane.Name = "panelEmpty1";
        pane.AutoHeight = true;
        pane.PaneStyle.BorderBottom.BorderWidth = System.Web.UI.WebControls.Unit.Pixel(1);
        pane.AutoWidth = false;
        pane.Size = Unit.Percentage(100);
        pane.SetContent(() =>
        {
            ViewContext.Writer.Write("<br/>");
        });

    });
    settings.Panes.Add(pane =>
    {
        pane.Name = "panelPobCurrent";
        pane.AutoHeight = true;
        pane.PaneStyle.BorderBottom.BorderWidth = System.Web.UI.WebControls.Unit.Pixel(1);
        pane.AutoWidth = false;
        pane.Size = Unit.Percentage(100);

        pane.SetContent(() =>
        {
            Html.RenderAction("PobCurrentPartial", "Personnel", Model);
        });
    });
}).GetHtml()

