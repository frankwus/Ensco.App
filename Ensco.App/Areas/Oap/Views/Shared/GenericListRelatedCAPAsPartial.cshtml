@model IEnumerable<Ensco.Irma.Oap.WebClient.Rig.IrmaCapa>

@{
    var genericListController = (dynamic)ViewContext.Controller;
}

@Html.DevExpress().GridView(settings =>
{
    genericListController.ConfigureCAPAGrid(settings, Html, ViewContext);
}).Bind(Model).GetHtml()
